---
import BaseLayout from "./base/Layout.astro";
import HeroNavBar from "./base/herobar/Common.astro";

export interface Props {
  title: string;
  thisHref: string;
}

const { title } = Astro.props;
const description = "大字根、字根归类";
const genLink = (label: string, relativeHref: string) => {
  return {
    label,
    href: import.meta.env.BASE_URL + relativeHref,
  };
};
const customTitle = title + " - 逸码 VV9"
---

<BaseLayout title={customTitle} description={description}>
  <HeroNavBar
    title={customTitle}
    subt={description}
    link={[
      genLink("说明", "VV9"),
      genLink("下载", "VV9/download"),
      genLink("字根练习", "VV9/zigen"),
      genLink("单字练习", "VV9/danzi"),
      genLink("反查", "VV9/search"),
    ]}
  />
  <div class="container is-max-desktop p-5 mt-2 content">
    <slot />
  </div>
</BaseLayout>
